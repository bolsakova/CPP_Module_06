#ifndef DATA_HPP
# define DATA_HPP

#include <string>

/**
 * @brief Non-empty structure used for serilization test.
 */
struct Data {
	int			id;
	double		value;
	std::string	label;	// shows that object is real
};

#endif
